models:
  my_new_model:
    template: openai
    model_name: gpt-4o-mini
    api_key_env: OPENAI_API_KEY
steps:
  planner: doubao_seed
  critic: doubao_seed
  graph_generator: doubao_seed
  researcher: doubao_seed
  summarizer: doubao_seed
  outliner: doubao_seed
  drafter: doubao_seed
  reviser: doubao_seed
  query_rewriter: doubao_seed
  chapter_summarizer: doubao_seed
  consistency_sentinel: doubao_seed
  chat_assistant: doubao_seed
embeddings:
  my_custom_embedding:
    template: openai
    model: text-embedding-3-large
    api_key_env: OPENAI_API_KEY
active_embedding_model: local_bge_embedding
writing_styles:
  academic_report: 采用严谨、客观的学术报告风格，使用专业术语，避免口语化表达。
  humorous_narrative: 以轻松幽默的口吻进行叙述，多用比喻、拟人等修辞手法，旨在逗乐读者。
re_rankers:
  my_reranker:
    template: sentence_transformers_reranker
    model_name: cross-encoder/ms-marco-MiniLM-L-6-v2
active_re_ranker_id: my_reranker
rag:
  recall_k: 8
  rerank_k: 3
active_text_splitter: my_semantic_splitter
